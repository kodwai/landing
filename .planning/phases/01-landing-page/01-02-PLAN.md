---
phase: 01-landing-page
type: execute
---

<objective>
Build a high-converting waitlist landing page that communicates the product vision and captures emails.

Purpose: This is the primary conversion surface — every marketing effort drives here.
Output: Complete landing page with hero, value props, how-it-works, social proof, and waitlist form.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
@~/.claude/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/02-content-arsenal/02-01-PLAN.md (messaging reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build hero section</name>
  <files>src/app/page.tsx, src/components/Hero.tsx</files>
  <action>
  Create a bold, dark-themed hero section:
  - Headline: "Technical Interviews for the AI Era" (or similar — punchy, clear)
  - Subheadline: "The first interview platform where candidates use Claude Code — a real AI coding agent, not a chatbot. Full session capture. AI-powered scoring. See how engineers actually work."
  - Waitlist email input + CTA button ("Join the Waitlist" / "Get Early Access")
  - Social proof line: "Join 500+ engineering leaders waiting for launch" (update dynamically later)
  - Background: subtle code/terminal aesthetic, dark gradient
  - Mobile responsive from the start

  Design principles: Clean, developer-focused, high contrast. Think Linear/Vercel aesthetic — not corporate HR software. No stock photos. Minimal animations.
  </action>
  <verify>Visual inspection on desktop and mobile viewports</verify>
  <done>Hero section renders correctly, email input is visible, CTA is prominent</done>
</task>

<task type="auto">
  <name>Task 2: Build value proposition sections</name>
  <files>src/components/ValueProps.tsx, src/components/HowItWorks.tsx, src/components/Differentiator.tsx</files>
  <action>
  Create three sections below the hero:

  **"Why AI Interview?" section** (3 cards):
  1. "Real AI Agent, Not a Chatbot" — Candidates use Claude Code with full terminal, file system, and agentic capabilities. Not a chat window with autocomplete.
  2. "Full Session Capture" — Every prompt, every edit, every terminal command. Complete transcript of how the candidate thinks with AI.
  3. "AI-Powered Scoring" — Automated evaluation of problem decomposition, AI control, verification habits, and communication.

  **"How It Works" section** (3 steps with icons):
  1. Set up interview → Define the challenge, configure AI access level
  2. Candidate codes with Claude Code → Live session, interviewer observes
  3. Review transcript & score → AI-generated evaluation with full replay

  **"Not Another Chat Widget" differentiator section**:
  Side-by-side comparison: "Other platforms" (basic chat, limited context, no file system) vs "AI Interview" (full Claude Code agent, terminal access, multi-file projects, session replay). Make it visual — two columns or a comparison table.

  Keep copy tight. No fluff. Every word earns its place.
  </action>
  <verify>All sections render, responsive on mobile, copy is readable</verify>
  <done>Three distinct sections communicate the value prop clearly</done>
</task>

<task type="auto">
  <name>Task 3: Build footer waitlist section and page footer</name>
  <files>src/components/BottomCTA.tsx, src/components/Footer.tsx</files>
  <action>
  Bottom CTA section: Second email capture form with urgency copy ("Be first to interview the AI-native way"). Same form behavior as hero.

  Footer: Minimal — logo, "Built for engineering teams that take AI seriously", Twitter/X link, email contact, copyright.

  Compose all sections in page.tsx in order: Hero → ValueProps → HowItWorks → Differentiator → BottomCTA → Footer.
  </action>
  <verify>Full page scrolls correctly, both email forms are functional (client-side for now), footer renders</verify>
  <done>Complete page with two CTAs and footer, fully scrollable and responsive</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete landing page with all sections</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: localhost:3000
    3. Scroll through entire page on desktop
    4. Check mobile viewport (Chrome DevTools responsive mode)
    5. Verify email inputs are visible and tappable
    6. Confirm copy is clear and compelling
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds
- [ ] All sections render on desktop and mobile
- [ ] Both email capture forms are present
- [ ] No horizontal scroll issues on mobile
- [ ] Copy is compelling and typo-free
</verification>

<success_criteria>
- Landing page communicates the product clearly in <30 seconds of reading
- Two email capture points (hero + bottom)
- Responsive on all viewports
- Developer-focused aesthetic (not corporate)
</success_criteria>

<output>
After completion, create `.planning/phases/01-landing-page/01-02-SUMMARY.md`
</output>
