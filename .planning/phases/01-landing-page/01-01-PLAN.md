---
phase: 01-landing-page
type: execute
---

<objective>
Set up the project, deploy to Vercel, and establish the development foundation.

Purpose: Get a deployable project running so all subsequent work is immediately live.
Output: Next.js project on Vercel with Tailwind CSS, ready for landing page development.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project with Tailwind</name>
  <files>package.json, tailwind.config.ts, app/layout.tsx, app/page.tsx</files>
  <action>Create Next.js 15 app with App Router, TypeScript, Tailwind CSS 4. Use `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"`. Keep it minimal — no extra dependencies yet.</action>
  <verify>npm run dev starts without errors, localhost:3000 shows default page</verify>
  <done>Project runs locally with Tailwind working</done>
</task>

<task type="auto">
  <name>Task 2: Deploy to Vercel</name>
  <files>vercel.json</files>
  <action>Deploy using Vercel CLI: `npx vercel --yes`. Link to Vercel project. Set up production deployment on main branch.</action>
  <verify>Vercel URL is accessible and shows the app</verify>
  <done>Live URL accessible, auto-deploys on push to main</done>
</task>

<task type="auto">
  <name>Task 3: Clean up defaults and add base structure</name>
  <files>src/app/page.tsx, src/app/layout.tsx, src/app/globals.css</files>
  <action>Remove Next.js boilerplate from page.tsx. Set up base layout with proper meta tags (title: "AI Interview — Technical Interviews with Claude Code", description). Add Inter font. Set up dark-mode-first color scheme. Create placeholder landing page with "Coming Soon" text.</action>
  <verify>npm run build succeeds, page renders clean placeholder</verify>
  <done>Clean project with proper meta tags, no boilerplate, ready for landing page build</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds without errors
- [ ] Vercel deployment is live
- [ ] Meta tags render correctly (check with curl or browser)
- [ ] No default Next.js boilerplate remains
</verification>

<success_criteria>
- Project builds and deploys successfully
- Live URL accessible
- Clean foundation ready for landing page development
</success_criteria>

<output>
After completion, create `.planning/phases/01-landing-page/01-01-SUMMARY.md`
</output>
